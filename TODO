* Graph Generation
  - check for existing weights before adding an edge
* Graph
   - Delete Edge
   - Shortest Path
   - Longest Path

* hashtable to store Graph pointer (one for every new connection) after generating graph
* logging
* storing, generated graphs in a log file